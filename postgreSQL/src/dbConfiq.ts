import { ClientConfig } from "pg";

export const dbConfig: ClientConfig = {
  user: "postgres",
  password: "Mayank@1813125",
  host: "localhost",
  port: 5432,
  database: "practice_postgres_with_sql",
};
